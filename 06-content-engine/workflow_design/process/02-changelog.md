# Process log — W2 As-is/To-be (thay đổi thiết kế)

> Đây là PROCESS, không phải OUTPUT — nhật ký lý do đổi hướng thiết kế, không phải deliverable của W2. Deliverable thật nằm ở `../output/02-as-is-tobe.md`.

## Sửa lại so với bản đầu (2026-08-09)

TH1 ban đầu thiết kế "gộp về đúng 1 lần quyết định" (chỉ HITL ở cổng duyệt cuối, tránh bắt người chọn 2 lần) — nhưng thực tế build cho thấy người dùng chờ mù ~2 phút (kèm tốn phí sinh ảnh) rồi mới biết AI viết về ý tưởng nào, không có cách sửa hướng nếu không thích. Đã đổi hướng: thêm 1 điểm quyết định RẺ (đọc 5 ý tưởng, chọn hoặc tự viết — không tốn phí ảnh, ~10s) TRƯỚC điểm quyết định ĐẮT (duyệt bài đã viết xong). Không sai lý thuyết HITL — chỉ đổi vị trí đặt cổng cho hợp lý hơn theo chi phí thật.
