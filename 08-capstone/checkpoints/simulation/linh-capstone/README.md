# Capstone Linh — Đối chiếu công nợ đầu tháng

Mở pitch.html trước. Package: brief + resource map (6 path thật) + d1 skill (test 4/4) + d2 workflow + e2e 5 assert + run-log 3 vòng + d3 app (unit test PASS — log re-run được, xem RUN.md) + d4 pitch + self-grading.md. Phần chưa runtime-test: liệt kê trong run-log. Ảnh demo: chưa có (runtime-test sẽ chụp).
