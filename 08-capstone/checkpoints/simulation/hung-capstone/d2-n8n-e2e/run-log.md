# Run Log — Hùng (legal-reviewer)

## Vòng 1 — 20/08
- Nghiệp vụ TRÙNG B4 (thẩm định hợp đồng) — mượn workflow gần nguyên vẹn, chỉ thay KB checklist (hợp đồng dịch vụ → xây dựng) + prompt R1-R8 + respond JSON. Áp F16/F17 + remap đầy đủ trong script build — đồ thị nguyên vẹn ngay lần đầu (không dính F15).
- Auto-check cấu trúc PASS.

## Phần CHƯA runtime-test
- Node schema validation giữa vẫn theo clause.schema.json của B4 — vì nghiệp vụ trùng nên CÓ THỂ GIỮ NGUYÊN (khác các bạn khác phải bỏ) — ghi chú rõ quyết định.
- Runtime AI ⏳ quota key (như cohort).
