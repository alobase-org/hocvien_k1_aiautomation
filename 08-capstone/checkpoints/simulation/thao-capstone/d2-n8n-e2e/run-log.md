# Run Log — Thảo (weekly-report)

## Vòng 1 — 20/08 (thiết kế)
- Đọc cảnh báo lab 02 (F16/F17 từ simulation các bạn trước) → **áp ngay từ đầu**: bỏ 2 node preprocessing B4, expression `$json.body.data`, respond JSON. Không dính lỗi đổi-tên-node vì remap connections trong script build.

## Vòng 2 — 21/08 (runtime thật trên n8n docker)
- Auto-check [3] PASS cấu trúc — xem kết quả trong `runtime-*` hoặc self-check.
